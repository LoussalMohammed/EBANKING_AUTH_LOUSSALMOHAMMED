services:
  postgres:
    container_name: roll_app
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=roll_app'
      - 'POSTGRES_USER=mohammed'
      - 'POSTGRES_PASSWORD=superman'
    ports:
      - '5433:5432'
